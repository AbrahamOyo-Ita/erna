import { NextResponse } from 'next/server'
import { apiError,assertSameOrigin,consumeLimit,requireAdmin,ApiError } from '@/lib/server/request'
export async function PATCH(request:Request,context:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const {user,admin}=await requireAdmin();await consumeLimit(user.id,'admin:moderate',120,3600);const {id}=await context.params;if(!/^[0-9a-f-]{36}$/i.test(id))throw new ApiError(400,'Invalid submission.');const b=await request.json();const decision=b.decision==='approved'?'approved':b.decision==='rejected'?'rejected':null;if(!decision)throw new ApiError(400,'Choose approve or reject.');const {data,error}=await admin.rpc('moderate_submission',{p_admin:user.id,p_submission:id,p_decision:decision,p_reason:b.reason??null,p_note:b.note??null});if(error)throw error;return NextResponse.json(data)}catch(error){return apiError(error)}}
